name: scaffloldbasics
description: A modern Flutter learning project demonstrating Scaffold, navigation, input, cards, and Material 3 patterns.
version: 2.0.0+2

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
